# ⚡ Institutional Trading Bot & Algorithmic Engine (MT5 / MQL5 / Python)

سیستم جامع و پروداکشن معاملات الگوریتمی و کمی برای **MetaTrader 5** به همراه ماژول‌های پایتون جهت شبیه‌سازی، پایش ریسک، اجرای سفارش، ماتریس همبستگی، تشخیص رژیم بازار، مدیریت ریسک هاف-کلی و تحلیل مونت‌کارلو.

---

## 🌟 ویژگی‌های کلیدی سیستم (InstitutionalTraderPro Ultra v5.50)

### ۱. معماری و اجرای سفارش (Order Execution & Architecture)
- توسعه‌یافته با کتابخانه رسمی استاندارد `Trade\Trade.mqh` و کلاس `CTrade`.
- **تشخیص خودکار حالت پر شدن سفارش بروکر (Auto Filling Mode Detection):** استعلام بیتی `SYMBOL_FILLING_MODE` و اعمال داینامیک حالات `FOK`، `IOC` یا `RETURN` جهت جلوگیری قطعی از خطای ۱۰۳۰ بروکر.
- پشتیبانی کامل از `MagicNumber` مجزا برای تمامی پوزیشن‌ها و دیل‌ها.
- **فیلتر کندل جدید (`IsNewBar`):** جلوگیری از پردازش‌های سنگین روی هر تیک و اجرای تحلیل فقط در ثانیه ابتدایی کندل تایم‌فریم معاملاتی.
- **مقاومت کامل در برابر کراش و قطعی (Crash Resilience):** خواندن مستقیم وضعیت سفارش‌ها از سرور بروکر بدون تکیه بر حافظه رم محلی.

### ۲. ماتریس همبستگی جفت‌ارزها (Correlation Matrix Guard)
- **جلوگیری از انباشت ریسک هم‌جهت روی یک ارز پایه یا مظنه (`IsCurrencyExposureExceeded`):**
  اگر پوزیشنی روی EURUSD باز باشد، ربات به طور خودکار باز شدن پوزیشن روی نمادهای هم‌جهت با دلار یا یورو (مانند GBPUSD یا USDCHF) را بلاک می‌کند تا سقف ریسک سبد ارزی همواره در محدوده ایمن باقی بماند.

### ۳. تشخیص رژیم بازار (Market Regime Detection)
- **فیلتر مومنتوم ADX جهت فیلتر کردن بازارهای فرسایشی و رنج (`IsTrendRegimeValid`):**
  معاملات شکست سطح فقط در شرایطی مجاز است که شاخص ADX بالای حد آستانه ۲۰ باشد؛ در بازارهای کم‌نوسان و فشرده (Choppy Squeeze)، اردرگذاری متوقف می‌شود تا از استاپ‌های فرسایشی جلوگیری شود.

### ۴. بهینه‌سازی ریسک پویا با معیار نیم‌کلی (Adaptive Half-Kelly)
- بر اساس ۵,۰۰۰ دور شبیه‌سازی استرس مونت‌کارلو روی ۱,۵۹۸ معامله تاریخی، سقف ریسک بهینه روی **۰.۷۵٪** تنظیم شده است.
- با فعال بودن حالت `InpUseHalfKellyAdaptive`، ربات وین‌ریت و نرخ سود به زیان ۳۰ معامله اخیر را پایش کرده و ریسک هر معامله را به صورت پویا بین ۰.۴٪ تا ۱.۵٪ تنظیم می‌کند.

### ۵. مدیریت پوزیشن و خروج نامتقارن (Asymmetric Exits)
- **حد ضرر با فضای تنفس (Breathing Room SL):** تنظیم استاپ‌لاس روی ۱.۵ برابر نوسان واقعی (ATR) جهت جلوگیری از سوختن معامله در پولبک‌ها و ریتست‌های طبیعی بازار.
- **سیو سود پله‌ای ۵۰٪ (Partial Take Profit):** بستن خودکار نیمی از حجم در سود ۱.۵ برابر ATR و واریز نقدی سود به بالانس.
- **ریسک‌فری قطعی (Break-Even):** انتقال اتوماتیک استاپ‌لاس نیمی دیگر به نقطه ورود بلافاصله پس از برداشت سود پله‌ای اول.
- **تریلینگ استاپ با تاخیر هوشمند:** فعال‌سازی تریلینگ استاپ پویا صرفاً پس از پیشروی حداقل ۲.۰ برابر ATR در سود.

### ۶. گاردهای امنیتی بازار، شبکه و وب‌هوک تلگرام
- **کلید قطع اضطراری افت حساب روزانه (`Daily Loss Kill-Switch`):** قفل خودکار معاملات در صورت افت بیش از ۳٪ از بالانس روزانه.
- **گارد اخبار مهم اقتصادی (High-Impact News Guard):** بررسی تقویم متاتریدر ۵ (`MqlCalendarValue`) و مسدودسازی ترید ۳۰ دقیقه قبل و بعد از رویدادهای قرمز.
- **هشدار و تله‌متری تلگرام:** ارسال آنی رویدادهای ورود، خروج ۵۰٪، تریلینگ و تریپ کیل‌سوئیچ به ربات تلگرام.

---

## 📊 ساختار ماژول‌ها و فایل‌های پروژه

```text
trading-bot/
├── InstitutionalTraderPro.mq5    # سورس کد اصلی و کامل اکسپرت به زبان MQL5 (Ultra v5.50)
├── InstitutionalTraderPro.ex5    # فایل باینری کامپایل‌شده بدون خطا برای متاتریدر ۵
├── AutoTraderPro.mq5             # نسخه اسکنر سبد نمادها (Multi-Currency Scanner)
├── telegram_telemetry_bot.py     # بات تله‌متری پایتون و مانیتورینگ تلگرام
├── monte_carlo_stress_test.json  # نتایج آزمون استحکام مونت‌کارلو (۵,۰۰۰ شبیه‌سازی)
├── ZmqBridgeEA.mq5               # اکسپرت پل ارتباطی ZeroMQ به پایتون
├── config.py                     # تنظیمات سراسری (نمادها، سشن‌ها، پارامترهای ریسک و پورت‌ها)
├── risk_manager.py               # هسته مدیریت ریسک، سایزینگ لات، Breakeven و سقف دراوداون
├── mt5_bridge.py                 # پل ارتباطی سوکت پایتون با MT5 و Fallback هوشمند
├── paper_simulator.py            # شبیه‌ساز دقیق تیک و اجرای سفارشات در پایتون
├── strategies/                   # استراتژی‌های الگوریتمی پایتون
├── web/                          # داشبورد وب گرافیکی و WebSocket
├── main.py                       # نقطه ورودی اصلی ربات پایتون
├── run_bot.sh                    # اسکریپت ران سریع سیستم
├── backtester.py                 # بکتستر استراتژی‌ها
└── README.md                     # مستندات کامل پروژه
```

---

## 🛠️ نحوه نصب و راه‌اندازی در MetaTrader 5

۱. نرم‌افزار MetaTrader 5 را باز کنید و از منوی بالا گزینه `File` و سپس `Open Data Folder` را انتخاب نمایید.
۲. وارد مسیر `MQL5/Experts` شوید.
۳. فایل‌های `InstitutionalTraderPro.mq5` و `InstitutionalTraderPro.ex5` را داخل این پوشه قرار دهید.
۴. در پنجره `Navigator` متاتریدر، روی شاخه `Expert Advisors` کلیک‌راست کرده و گزینه `Refresh` را بزنید.
۵. اکسپرت `InstitutionalTraderPro` را روی چارت مورد نظر (مثلاً EURUSD تایم‌فریم H1) درگ کنید.
۶. در پنجره تنظیمات تب `Common`، تیک گزینه **`Allow Algo Trading`** را فعال کرده و دکمه `OK` را بزنید.
۷. مطمئن شوید دکمه `Algo Trading` در نوار ابزار اصلی بالای متاتریدر روشن و سبز رنگ باشد.
